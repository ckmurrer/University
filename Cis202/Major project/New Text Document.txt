tree search for the leaderboard at the end of each level to display then use a sort method to input the latest time into the tree an if its slower than the rest of
the times it will get rid of it. if it is greater than a value within the tree it will sort it to place within correct possition an the last position will get cut from the tree